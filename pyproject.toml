[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sapfhir"
version = "0.1.0"
description = "SAP IS-H/i.s.h.med -> FHIR R4 read-only secondary-use export, dashboard, MCP server"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
